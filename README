GB keyboard with Czech accents.
2345667890 were changed to ěščřžýáíé, = with modificator is ´, with two modificators it is ˇ, ; with modificator is ů
Also modificator + any of [n, t, u, o] will write equivalent [ň, ť, ú, ó]

This change was done for Arch Linux (03/2016)
--------
To apply patch changes to `cz` in `/usr/share/X11/xkb/symbols/` and `base.xml` and `evdev.xml` in `/usr/share/X11/xkb/rules`

(or use provided `patch.sh` to autoapply - WARNING: it's just a one-off script - probably doesn't work very well)
